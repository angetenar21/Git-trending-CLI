# Trending Repos CLI

A simple Node.js command-line tool to fetch trending GitHub repositories over a given time duration.

## Installation

Clone the repo and install dependencies:

```bash
git clone <your-repo-url>
cd trending-repos-cli
npm install
npm link